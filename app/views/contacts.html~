<div class="row">
  <div class="col-lg-6">
    <pre> dataService: {{ dataService|json }}</pre>
    <ul>
      <li ng-repeat="person in people track by person.id">
        <div ng-click="editPerson(person.id)" ng-class="{active: activePerson.id==person.id}">{{ person.name }}: {{person.age }}</div>
      </li>
      <li><button ng-click="editPerson()">Add Person</button></li>
    </ul>
  </div>

  <div class="col-lg-6">
    <ng-form name="personForm" novalidate>
      <label> Name <input type="text" name="theName" ng-model="activePerson.name" ng-required="true"></label>
      <label> Age <input type="number" name="theAge" ng-model="activePerson.age" ng-required="true"></label>
      <input type="hidden" ng-model="activePerson.id">
      <button ng-click="saveActivePerson()"> Save</button>
    </ng-form>
  </div>
</div>
